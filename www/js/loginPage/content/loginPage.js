(function () {
  'use strict';

  angular
    .module('loginPage.ctrl', [])
    .controller('loginPageCtrl', ['$scope', '$rootScope', '$timeout', '$interval',
      function ($scope, $rootScope, $timeout, $interval) {
        //variables
        var self = this;

        
      }]);
})();
