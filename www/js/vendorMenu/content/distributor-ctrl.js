/**
 * Created by user on 6/3/2016.
 */
(function () {
  'use strict';

  angular
    .module('distributor.ctrl', [])
    .controller('DistributorCtrl', ['$scope', '$rootScope', '$timeout', '$interval', '$window', '$ionicActionSheet',
      function ($scope, $rootScope, $timeout, $interval, $window, $ionicActionSheet) {
        //variables
        var self = this;
        
      }
    ])
})();
