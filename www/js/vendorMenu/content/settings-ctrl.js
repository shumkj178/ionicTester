/**
 * Created by user on 5/20/2016.
 */
(function () {
  'use strict';

  angular
    .module('settings.ctrl', [])
    .controller('SettingsCtrl', ['$scope', '$rootScope', '$timeout', '$interval', '$window',
      function ($scope, $rootScope, $timeout, $interval, $window) {
        //variables
        var self = this;

      }
    ])
})();
